---
import Layout from "../layouts/Layout.astro";
import SnippetViewer from "../components/SnippetViewer";

const { id } = Astro.params;
---

<Layout>
    <SnippetViewer client:only="react" publicId={id || ""} />
</Layout>
