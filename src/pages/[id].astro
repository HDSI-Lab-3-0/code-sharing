---
import Layout from "../layouts/Layout.astro";
import SnippetViewerRoot from "../components/SnippetViewerRoot";

const { id } = Astro.params;
---

<Layout>
    <SnippetViewerRoot client:only="react" publicId={id || ""} />
</Layout>
